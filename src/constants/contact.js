import { SITE } from "../config/site";

export const CONTACT = {
  PHONE_DISPLAY: "+968 0000 0000",
  PHONE_TEL: SITE.phoneIntl,
  WHATSAPP: SITE.phoneWhatsApp,
  EMAIL: SITE.email,
  MAPS_QUERY: `${SITE.coords.lat},${SITE.coords.lng}`,
};
